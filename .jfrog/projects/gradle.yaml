version: 1
type: gradle
resolver:
  repo: mvn
  serverId: artifactory
deployer:
  repo: v1beta2-demo-2-zoxIE-mvn-sandbox-local
  serverId: artifactory
  artifactPattern: '[organization]/[module]/[revision]/[artifact]-[revision](-[classifier]).[ext]'
useWrapper: true
